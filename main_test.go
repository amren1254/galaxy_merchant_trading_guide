package main

import (
	"testing"
)

func TestOpenAndReadFile(t *testing.T) {
	// t.Run("test reading file", func(t *testing.T) {
	// 	if _, err := readFile(strings.NewReader("This is a test string")); (err != nil) != false {
	// 		t.Errorf("error opening and reading input file : %v", err)
	// 	}
	// })
	// var buffer bytes.Buffer
	// buffer.WriteString("fake, input, data")
	// content, err := readFile(&buffer)
	// if err != nil {
	// 	t.Error("Failed to read input text data")
	// }
	// fmt.Print(content)
}
